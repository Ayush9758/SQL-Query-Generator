// tailwind.config.js — kept for reference but not actively used
// The project uses Pure CSS with cosmic custom design system
export default {
  content: ['./src/**/*.{js,jsx}'],
  theme: { extend: {} },
  plugins: [],
}
